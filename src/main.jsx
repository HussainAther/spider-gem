import React from "react";
import ReactDOM from "react-dom/client";
import SpiderGem from "./SpiderGem.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SpiderGem />
  </React.StrictMode>
);

